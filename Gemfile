source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.7.0'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~> 6.0.3', '>= 6.0.3.2'
gem 'rails-helper'
# Use Puma as the app server
gem 'puma', '~> 4.1'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
# gem 'jbuilder', '~> 2.7'
# Use Redis adapter to run Action Cable in production
gem 'redis', '~> 4.0'
# Use Active Model has_secure_password
gem 'bcrypt', '~> 3.1.7'

# Reduces boot times through caching; required in config/boot.rb
gem 'bootsnap', '>= 1.4.2', require: false

# Use Rack CORS for handling Cross-Origin Resource Sharing (CORS), making cross-origin AJAX possible
# gem 'rack-cors'

gem 'rspec'
gem 'rspec-rails'

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug', platforms: [:mri, :mingw, :x64_mingw]
end

group :development do
  gem 'listen', '~> 3.2'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.0.0'
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]



# source 'https://rubygems.org'
# ruby '2.3.0' # You can use whenever version you want, but we want to be on edge

# gem 'actionpack',    '~> 4.2.5' # For your favorite views
# gem 'actionview',    '~>4.2.5'
# gem 'activemodel',    '~> 4.2.5' # Models without activerecord.
# gem 'activesupport',  '~> 4.2.5' # All things we like
# gem 'bootstrap-sass', '~> 3.3.6' # You can use other frontend framework, but bootstrap is soo.. friendly
# gem 'sprockets', '>= 3.0.0'  # Sprokets!

# gem 'sass-rails', '~> 5.0'  # For styles, just if you need views)
# gem 'uglifier', '>= 1.3.0' # Obfuscate this beautiful js
# gem 'haml-rails' # It’s really that simple to write better views with this gem
# gem 'dotenv-rails'  # This gem will allow you to store all of your ENV in nasty .env file

# group :test do
#   gem 'simplecov', require: false # testing is important? Have you forgot already? And coverage reporter give you more insights then everyone else
# end

# group :development, :test do
#   gem 'faker' # Fake data! More fake data!

#   gem 'pry-rails'  # I hate irb. But if you like it. Just remove...

#   gem 'rspec-rails', '~> 3.0' # Testing, testing...
# end

# group :development do
#   gem 'guard-rspec', require: false  # Run your specs automatically. Yeah, baby...

#   gem 'guard-rubocop' # Remove every code smell with simple commands

#   gem 'rubocop-rspec'

#   gem 'quiet_assets' # Assets are so loud sometimes. Just make sure you remove some noise
# end